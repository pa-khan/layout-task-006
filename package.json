{
  "name": "001",
  "version": "1.0.0",
  "description": "* Иницировать пакеры, в терминале npm i * Запустить browsersync, в терминале gulp watch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "gulp": "^4.0.2"
  }
}
